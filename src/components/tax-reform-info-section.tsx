"use client";

/**
 * Esta seção foi desativada conforme solicitação para limpar o visual do simulador.
 * Retornamos 'null' para que o componente não renderize nada na tela,
 * mantendo a integridade das importações em outros arquivos.
 */
export default function TaxReformInfoSection() {
  return null;
}